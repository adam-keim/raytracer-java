package main.shapes;

import main.*;

import java.util.ArrayList;

public class Csg extends Shape {
    // TODO: WRITE CSG TESTS FROM BOOK AND BOUNDING BOXES

    @Override
    public ArrayList<Intersection> localIntersect(Ray r) {
        return null;
    }

    @Override
    public Vector localNormalAt(Point point, Intersection hit) {
        return null;
    }

    @Override
    public BoundingBox boundsOf() {
        return null;
    }
}
